# npm

In the context of the command "npm i -D eslint", the "i" is a shorthand for "install". So, "npm i -D eslint" and "npm install -D eslint" are equivalent commands and will both install the "eslint" package as a development dependency (-D flag) in your project's node_modules directory.

<br>

```json
npm init --
```

